# Copy this file to .env and edit as needed
# cp .env.example .env

FLASK_ENV=development
FLASK_DEBUG=True

# SQLite (default — stored in backend folder)
DATABASE_URI=sqlite:///diagnostics.db

# To use PostgreSQL in production:
# DATABASE_URI=postgresql://user:password@localhost/car_diagnostic
